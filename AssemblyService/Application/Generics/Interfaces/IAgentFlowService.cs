namespace Application.Generics.Interfaces
{
    public interface IAgentFlowService
    {
        // Task<AgentFlow> CreateOffer(bool isError, CreateOfferCommand request, dynamic response);
        // Task<AgentFlow> UpdateOffer(bool isError, UpdateOfferCommand request, dynamic response);
        // Task<AgentFlow> CreateApplication(bool isError, CreateApplicationCommand request, dynamic response);
        // Task<AgentFlow> UpdateParty(bool isError, UpdatePartyCommand request, dynamic response);
    }
}
